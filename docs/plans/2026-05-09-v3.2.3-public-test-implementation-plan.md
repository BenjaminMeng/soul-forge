# Soul Forge v3.2.3 Public Test Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ship a minimal v3.2.3 public stability patch over v3.2.2 for small friend testing.

**Architecture:** Keep the public repository on the existing v3.2.x line. Add a narrow release smoke test that locks version metadata and tester-facing wording, then update only the required metadata/docs/constant files to pass it. Do not sync internal P1-B/P1-C code.

**Tech Stack:** Node.js CommonJS scripts, Markdown docs, JSON metadata, Git.

---

### Task 1: Add Public Release Smoke Test

**Files:**
- Create: `tests/Test-PublicRelease-v3.2.3.js`

**Step 1: Write the failing test**

Create a Node.js test that reads real repository files and asserts:

- `version.json.version === "3.2.3"`
- `package.json.version === "3.2.3"`
- `hooks/soul-forge-bootstrap/handler.js` contains `SOUL_FORGE_VERSION = '3.2.3'`
- `README.md` version badge contains `version-3.2.3`
- `README.md` includes `public stable test build`
- `README.md` includes `preinstalled`
- `CHANGELOG.md` has a `## v3.2.3` entry
- `version.json.files` still does not include internal-only `p1b/` or `p1c/` files
- repository root does not contain tracked internal-only directories `p1b` or `p1c`

**Step 2: Run test to verify it fails**

Run:

```powershell
node tests\Test-PublicRelease-v3.2.3.js
```

Expected: FAIL because the public repo is still v3.2.2 and `package.json` is still v3.1.1.

### Task 2: Update Version Metadata

**Files:**
- Modify: `version.json`
- Modify: `package.json`
- Modify: `hooks/soul-forge-bootstrap/handler.js`

**Step 1: Apply minimal metadata edits**

- Set `version.json.version` to `3.2.3`.
- Set `version.json.min_handler_version` to `3.2.3`.
- Set `version.json.released_at` to `2026-05-09T00:00:00Z`.
- Set `version.json.changelog` to a short public stability patch summary.
- Set `package.json.version` to `3.2.3`.
- Set handler `SOUL_FORGE_VERSION` to `3.2.3`.

Do not add new files to `version.json.files`.

**Step 2: Run release smoke**

Run:

```powershell
node tests\Test-PublicRelease-v3.2.3.js
```

Expected: still FAIL until README and changelog are updated.

### Task 3: Update Tester-Facing Docs

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

**Step 1: Apply minimal docs edits**

In README:

- Update badge from `3.1.1` to `3.2.3`.
- Add a short public test note near the top.
- Mention preinstalled OpenClaw agents without rewriting the full docs.
- Keep existing install instructions mostly intact.

In CHANGELOG:

- Add `## v3.2.3 — 2026-05-09`.
- Explain this is a public stability test patch.
- Mention metadata consistency and installer smoke verification.
- Do not claim internal P1-B/P1-C features.

**Step 2: Run release smoke**

Run:

```powershell
node tests\Test-PublicRelease-v3.2.3.js
```

Expected: PASS.

### Task 4: Verify Syntax and Public Scope

**Files:**
- No expected production edits.

**Step 1: Run syntax checks**

Run:

```powershell
node --check installer.js
node --check hooks\soul-forge-bootstrap\handler.js
```

Expected: both commands exit 0.

**Step 2: Check git diff scope**

Run:

```powershell
git status --short
git diff --stat
```

Expected changed files are limited to:

- `docs/plans/2026-05-09-v3.2.3-public-test-purpose.md`
- `docs/plans/2026-05-09-v3.2.3-public-test-implementation-plan.md`
- `tests/Test-PublicRelease-v3.2.3.js`
- `version.json`
- `package.json`
- `README.md`
- `CHANGELOG.md`
- `hooks/soul-forge-bootstrap/handler.js`

No `p1b/`, `p1c/`, `.tmp-*`, `node_modules/`, or internal evidence files should appear.

### Task 5: Final Readiness Note

**Files:**
- No expected production edits.

**Step 1: Summarize verification evidence**

Record in the final response:

- Release smoke result.
- Syntax check result.
- Scope check result.
- Any tests not run.

**Step 2: Leave push/commit explicit**

Do not push unless the maintainer asks. If committing is requested later, commit the narrow patch as:

```powershell
git add docs/plans tests version.json package.json README.md CHANGELOG.md hooks/soul-forge-bootstrap/handler.js
git commit -m "chore: prepare v3.2.3 public test release"
```
